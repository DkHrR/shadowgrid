pragma language_version 0.23;

import CompactStandardLibrary;

export ledger hashes: Map<Bytes<32>, Bytes<32>>;

export circuit test_hash(
  key: Bytes<32>,
  input: Bytes<32>
): [] {
  const hash_val = persistentHash<Bytes<32>>(input);
  hashes.insert(disclose(key), disclose(hash_val));
}
